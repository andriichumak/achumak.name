/**
 * Created by andy on 12-Jul-16.
 */
import { fork } from 'redux-saga/effects';

export default function* () {
	yield [
		// fork(sessionSaga),
		// fork(messageSage),
		// fork(user),
		// fork(roles),
		// fork(productTypes),
		// fork(contract),
		// fork(destinations)
	];
}
